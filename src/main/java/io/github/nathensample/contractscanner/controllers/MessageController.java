package io.github.nathensample.contractscanner.controllers;

public class MessageController {
    //Responsible for checking a users subscription settings against the current update
    //Dispatch to the discord side to perform work once complete.
}
